import 'package:flutter/material.dart';

class StudentChatListWidget extends StatelessWidget {
  const StudentChatListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
